export const PADDING: number = 12;
export const TITLE_FONT_SIZE: number = 20;
export const SUBTITLE_FONT_SIZE: number = 12;
export const MARGIN_HORIZONTAL: number = 8;

// Web layout: cap content width on large desktop screens and keep slight side
// margins so the phone-first UI does not stretch edge-to-edge in the browser.
export const WEB_MAX_WIDTH: number = 1200;
export const WEB_GUTTER: number = 16;
// Narrower inner cap for stacked, text-heavy widgets (team list, analysis cards)
// so they read as cards instead of full-bleed bars within the wide page.
export const WEB_CONTENT_WIDTH: number = 820;
